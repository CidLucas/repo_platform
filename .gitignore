# Ambientes Virtuais
.venv/
venv/
**/.venv/
.secrets-venv/

# Google Cloud / Service Account credentials (never commit real keys!)
**/analytics-big-query*.json
**/service-account*.json
**/gcp-credentials.json
service_account.json
!**/*example*.json

# Test output logs (never commit)
*.log
!*.changelog.md

# Arquivos de Configuração de IDEs
.vscode/
.idea/

# Arquivos de ambiente
.env
*.env.local
.env.production
.env.prod
.env.cloudrun
.trivyignore
# Ignorar arquivos de sistema do macOS
# Arquivos de Sistema do macOS
.DS_Store
*/.DS_Store

# Arquivos de Cache e Compilados do Python
__pycache__/
*.pyc
*.pyo
*.pyd
*/**/__pycache__/
*/**/*.pyc

# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
terraform.tfvars
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Build and deployment scripts (local only)
build-and-push-images.sh
build-single-image.sh
tag-and-push-images.sh
pull-github-push-gcp.sh
# Documentation (local only — single source of truth, not versioned in git)
/docs/

# GitHub configuration and team instructions (not part of portfolio)
.github/copilot-instructions.md

# Seeds and project data (large files, local only)
seeds/
projetos/
open-design/
.vercel

# Local debug/test artifacts (root)
output.json
results.json
test.html
teste_sumsub.json
termos_formatados.md
tmp_normalize_prompts.py

# Langfuse self-hosted (local only)
langfuse/
.ssh/
*.pem
.env
.hermes/
.coverage
