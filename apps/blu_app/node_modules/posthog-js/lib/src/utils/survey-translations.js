"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.detectUserLanguage = detectUserLanguage;
exports.applySurveyTranslation = applySurveyTranslation;
exports.applySurveyTranslationForUser = applySurveyTranslationForUser;
var constants_1 = require("../constants");
var logger_1 = require("./logger");
var event_utils_1 = require("./event-utils");
var language_utils_1 = require("./language-utils");
var core_1 = require("@posthog/core");
var logger = (0, logger_1.createLogger)('[SurveyTranslations]');
function getLanguageFromStoredPersonProperties(storedPersonProperties) {
    if (!storedPersonProperties ||
        typeof storedPersonProperties !== 'object' ||
        !('language' in storedPersonProperties)) {
        return null;
    }
    var personLanguage = storedPersonProperties.language;
    return typeof personLanguage === 'string' && personLanguage.trim() ? personLanguage.trim() : null;
}
/**
 * Detects the user's language using priority order:
 * 1. config.override_display_language (explicit override)
 * 2. person properties 'language' (allows programmatic control via posthog.identify())
 * 3. navigator.language (browser language)
 *
 * TODO: Consider adding dynamic language change detection in the future:
 * - Listen to 'languagechange' event on window (https://developer.mozilla.org/en-US/docs/Web/API/Window/languagechange_event)
 * - Listen to config changes (once we add config change events to PostHog core)
 * - Re-render survey when language changes mid-session
 *
 * @param instance - PostHog instance to retrieve config and person properties
 * @returns The detected language code (e.g., 'fr', 'es', 'en-US') or null if not found
 */
function detectUserLanguage(instance) {
    var configLanguage = instance.config.override_display_language;
    if (configLanguage) {
        logger.info("Using config.override_display_language: ".concat(configLanguage));
        return configLanguage;
    }
    var personLanguage = getLanguageFromStoredPersonProperties((0, core_1.isFunction)(instance.get_property) ? instance.get_property(constants_1.STORED_PERSON_PROPERTIES_KEY) : undefined);
    if (personLanguage) {
        logger.info("Using person property language: ".concat(personLanguage));
        return personLanguage;
    }
    var browserLanguage = (0, event_utils_1.getBrowserLanguage)();
    if (browserLanguage) {
        logger.info("Using browser language: ".concat(browserLanguage));
        return browserLanguage;
    }
    logger.info('No user language detected');
    return null;
}
/**
 * Merges translated question fields on top of default question fields
 * @param question - The original question object
 * @param targetLanguage - The target language code
 * @returns An object with the translated question and the matched language key
 */
function mergeQuestionTranslation(question, targetLanguage) {
    var _a;
    var translationKey = (0, language_utils_1.findBestTranslationMatch)(question.translations, targetLanguage);
    if (!translationKey) {
        return { question: question, matchedKey: null, hasChanges: false };
    }
    var questionTranslation = (_a = question.translations) === null || _a === void 0 ? void 0 : _a[translationKey];
    if (!questionTranslation) {
        return { question: question, matchedKey: null, hasChanges: false };
    }
    var translated = __assign({}, question);
    var hasChanges = false;
    if (!(0, core_1.isUndefined)(questionTranslation.question)) {
        translated.question = questionTranslation.question;
        hasChanges = true;
    }
    if (!(0, core_1.isUndefined)(questionTranslation.description)) {
        translated.description = questionTranslation.description;
        hasChanges = true;
    }
    if (!(0, core_1.isUndefined)(questionTranslation.buttonText)) {
        translated.buttonText = questionTranslation.buttonText;
        hasChanges = true;
    }
    if ('link' in translated && !(0, core_1.isUndefined)(questionTranslation.link)) {
        translated.link = questionTranslation.link;
        hasChanges = true;
    }
    if ('lowerBoundLabel' in translated && !(0, core_1.isUndefined)(questionTranslation.lowerBoundLabel)) {
        translated.lowerBoundLabel = questionTranslation.lowerBoundLabel;
        hasChanges = true;
    }
    if ('upperBoundLabel' in translated && !(0, core_1.isUndefined)(questionTranslation.upperBoundLabel)) {
        translated.upperBoundLabel = questionTranslation.upperBoundLabel;
        hasChanges = true;
    }
    if ('choices' in translated && isTranslatedChoices(questionTranslation)) {
        translated.choices = questionTranslation.choices;
        hasChanges = true;
    }
    return {
        question: hasChanges ? translated : question,
        matchedKey: hasChanges ? translationKey : null,
        hasChanges: hasChanges,
    };
}
function isTranslatedChoices(questionTranslation) {
    return (0, core_1.isArray)(questionTranslation.choices);
}
function hasThankYouTranslation(translation) {
    return (!(0, core_1.isUndefined)(translation.thankYouMessageHeader) ||
        !(0, core_1.isUndefined)(translation.thankYouMessageDescription) ||
        !(0, core_1.isUndefined)(translation.thankYouMessageCloseButtonText));
}
/**
 * Applies translations to a survey based on the target language
 * @param survey - The original survey object
 * @param targetLanguage - The target language code
 * @returns An object with the translated survey and the matched language key (or null if no match)
 */
function applySurveyTranslation(survey, targetLanguage) {
    var _a;
    var translationKey = (0, language_utils_1.findBestTranslationMatch)(survey.translations, targetLanguage);
    var translated = __assign({}, survey);
    var hasTranslation = false;
    if (translationKey) {
        var translation = (_a = survey.translations) === null || _a === void 0 ? void 0 : _a[translationKey];
        if (translation) {
            logger.info("Applying survey-level translation for language: ".concat(translationKey));
            if (!(0, core_1.isUndefined)(translation.name)) {
                translated.name = translation.name;
                hasTranslation = true;
            }
            if (translated.appearance) {
                translated.appearance = __assign({}, translated.appearance);
                if (!(0, core_1.isUndefined)(translation.thankYouMessageHeader)) {
                    translated.appearance.thankYouMessageHeader = translation.thankYouMessageHeader;
                    hasTranslation = true;
                }
                if (!(0, core_1.isUndefined)(translation.thankYouMessageDescription)) {
                    translated.appearance.thankYouMessageDescription = translation.thankYouMessageDescription;
                    hasTranslation = true;
                }
                if (!(0, core_1.isUndefined)(translation.thankYouMessageCloseButtonText)) {
                    translated.appearance.thankYouMessageCloseButtonText = translation.thankYouMessageCloseButtonText;
                    hasTranslation = true;
                }
            }
            else if (hasThankYouTranslation(translation)) {
                // Nothing renders without appearance, but the locale did match a valid runtime translation.
                hasTranslation = true;
            }
        }
    }
    // Always try to apply question-level translations (each question has its own translations field)
    var translatedResults = survey.questions.map(function (question) { return mergeQuestionTranslation(question, targetLanguage); });
    var translatedQuestions = translatedResults.map(function (r) { return r.question; });
    var anyQuestionTranslated = translatedResults.some(function (r) { return r.hasChanges; });
    // Track the first matched key from question translations if we don't have a survey-level match
    var questionMatchedKey = null;
    if (!translationKey) {
        var firstMatch = translatedResults.find(function (r) { return r.matchedKey; });
        questionMatchedKey = (firstMatch === null || firstMatch === void 0 ? void 0 : firstMatch.matchedKey) || null;
    }
    if (anyQuestionTranslated) {
        translated.questions = translatedQuestions;
        hasTranslation = true;
        logger.info("Applied question-level translations for language: ".concat(targetLanguage));
    }
    return {
        survey: translated,
        matchedKey: hasTranslation ? translationKey || questionMatchedKey : null,
    };
}
/**
 * Applies translations to a survey based on the user's language from person properties
 * @param survey - The original survey object
 * @param instance - PostHog instance to retrieve person properties
 * @returns An object containing the translated survey and the language used (or null if no translation applied)
 */
function applySurveyTranslationForUser(survey, instance) {
    var userLanguage = detectUserLanguage(instance);
    if (!userLanguage) {
        logger.info('No user language detected');
        return { survey: survey, language: null };
    }
    var result = applySurveyTranslation(survey, userLanguage);
    return {
        survey: result.survey,
        language: result.matchedKey,
    };
}
//# sourceMappingURL=survey-translations.js.map