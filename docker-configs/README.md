# Docker Configurations

This directory contains configuration files for various Docker containers used in the Vizu project.

## OpenTelemetry Collector

The `otel-collector` directory contains the configuration for the OpenTelemetry Collector, which is used for gathering and exporting telemetry data (metrics, traces, and logs) from the various services in the application.
