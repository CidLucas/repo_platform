[tool.poetry]
name = "vizu_landing_intel"
version = "0.1.0"
description = "Website intel extraction for onboarding package proposal"
authors = ["Lucas Cid <lucas@vizubr.com>"]
packages = [{ include = "vizu_landing_intel", from = "src" }]

[tool.poetry.dependencies]
python = "^3.11"
httpx = "^0.27.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"
ruff = "^0.14.7"

[tool.pytest.ini_options]
pythonpath = ["src", "../../libs"]
