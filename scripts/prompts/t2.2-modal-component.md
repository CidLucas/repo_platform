# T2.2 — Criar componente Modal compartilhado

## Contexto

O spec (seção 11.3 "Modais & overlays") define o padrão de modais usando `.intg-modal` do global.css. O CSS já tem:

- `.intg-modal` — display:none, fixed, inset:0, z-index:400, background, backdrop-filter
- `.intg-modal.open` — display:flex
- `.intg-box` — box estilizada (420px, padding, bg, radius, shadow)
- `.modal-acts` — flex row com gap, justify-content:flex-end

Hoje ~12 overlays são hand-rolled duplicando backdrop, posicionamento e fechamento (AdminScreen ×11, IntegrationModal ×4, etc.).

## O que fazer

Criar `apps/blu_v3/src/components/shared/Modal.tsx`:

```tsx
import { useEffect, useCallback, type ReactNode } from 'react'
import { createPortal } from 'react-dom'

interface ModalProps {
  open: boolean
  onClose: () => void
  title?: string
  subtitle?: string
  children: ReactNode
  /** Largura máxima da box (default 420px) */
  width?: string
}

export default function Modal({ open, onClose, title, subtitle, children, width = '420px' }: ModalProps) {
  // Fechar com Escape
  const handleKey = useCallback((e: KeyboardEvent) => {
    if (e.key === 'Escape') onClose()
  }, [onClose])

  useEffect(() => {
    if (open) {
      document.addEventListener('keydown', handleKey)
      return () => document.removeEventListener('keydown', handleKey)
    }
  }, [open, handleKey])

  // Fechar ao clicar no backdrop (não na box interna)
  const handleBackdrop = (e: React.MouseEvent) => {
    if (e.target === e.currentTarget) onClose()
  }

  if (!open) return null

  return createPortal(
    <div className="intg-modal open" onClick={handleBackdrop}>
      <div className="intg-box" style={{ width, maxWidth: '93vw' }}>
        {title && (
          <div style={{ marginBottom: subtitle ? 3 : 17 }}>
            <h3 style={{ fontSize: 15, fontWeight: 700 }}>{title}</h3>
            {subtitle && <p className="msub">{subtitle}</p>}
          </div>
        )}
        {children}
      </div>
    </div>,
    document.body
  )
}
```

**Design decisions:**
- Usa `createPortal` para renderizar no body — evita problemas de z-index stacking context
- Fecha com Esc (keydown) e backdrop-click (target === currentTarget)
- Retorna `null` quando `!open` em vez de esconder com CSS — evita render desnecessário
- Usa classe `.intg-modal.open` do global.css (que já tem backdrop blur + animação)
- Usa `.intg-box` do global.css (box estilizada)
- `width` prop opcional para customizar largura
- Não depende de nenhuma dependência externa

## Verificação

1. `npx tsc --noEmit` — deve compilar
2. `createPortal` do react-dom importado (sem dependência extra)
3. Props: open (obrigatório), onClose (obrigatório), title? (string), subtitle? (string), children (obrigatório), width? (string, default '420px')

## Arquivos

- `apps/blu_v3/src/components/shared/Modal.tsx` — CRIAR

## Não fazer

- ❌ Não modificar classes CSS existentes (já temos .intg-modal)
- ❌ Não adicionar dependências novas
- ❌ Não modificar nenhum componente existente (a migração é Fase 4)
