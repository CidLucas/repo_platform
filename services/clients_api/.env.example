# Copie este arquivo para .env e preencha os valores
# Para gerar uma chave: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIALS_ENCRYPTION_KEY="sua_chave_fernet_gerada_aqui"