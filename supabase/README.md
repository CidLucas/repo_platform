# Supabase

This directory contains configurations and migrations for the Supabase instance used by the Vizu application.

## Migrations

The `migrations` directory contains SQL files that are used to manage the database schema of the Supabase database. These migrations are applied automatically by Supabase when they are pushed to the repository.
