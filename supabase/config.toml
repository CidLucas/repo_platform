# A string used to distinguish different Supabase projects on the same host. Defaults to the working
# directory name when running `supabase init`.
project_id = "haruewffnubdgyofftut"

[api]
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. public and storage are always included.
schemas = ["public", "analytics_v2", "vector_db"]
# Extra schemas to add to the search_path of every request.
extra_search_path = ["public", "extensions", "vector_db"]
# The maximum number of rows returns from a view, table, or stored procedure. Limits payload size
# for accidental or malicious requests.
max_rows = 1000

[functions]
# JWT verification enabled on all edge functions for security
# Functions that need public access should validate auth internally

[functions.match-columns]
verify_jwt = true

[functions.embed]
verify_jwt = true

[functions.process-document]
verify_jwt = true

[functions.search-documents]
verify_jwt = true

[functions.run-sync]
verify_jwt = false

[functions.google-calendar-events]
verify_jwt = true

[functions.onboarding-bootstrap]
verify_jwt = true

[functions.onboarding-capture-drive-token]
verify_jwt = true
