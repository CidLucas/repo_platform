# match-columns: stateless fuzzy schema matcher — pure computation, no DB writes.
# verify_jwt = true: Supabase edge validates the caller's JWT before invoking
# the function. No auth code is needed inside the function body because it
# performs no database operations and holds no tenant data.
verify_jwt = true
